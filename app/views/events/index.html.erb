<h2>Events</h2>
<h3><%= link_to 'Event新規作成', '/events/new', method: :get %></h3><br>

<% if @events != nil %>
	<% @events.each do |event| %>
		・<%= link_to event.event, "/events/#{event.id}", method: :get %><br>
	<% end %>
<% else %>
	<p>イベントはまだありません。</p>
<% end %>